# Docker Terraria Server Configuration
# Copy this file to .env and modify as needed

# ===========================================
# BASIC SERVER CONFIGURATION
# ===========================================

# Server port (default: 7777)
SERVER_PORT=7777

# Maximum number of players (default: 16)
MAX_PLAYERS=16

# Server password (leave empty for no password)
SERVER_PASSWORD=

# Server language (default: en-US)
LANGUAGE=en-US

# Enable autosave (1=yes, 0=no) (default: 1)
AUTOSAVE=1

# Container timezone (default: Asia/Shanghai)
TZ=Asia/Shanghai

# ===========================================
# BUILD CONFIGURATION
# ===========================================

# Use local Terraria server files instead of downloading (1=yes, 0=no) (default: 0)
# If set to 1, place Terraria server files in docker/terraria-server/ directory
USE_LOCAL_FILES=0

# Terraria version to download (default: 1449)
TERRARIA_VERSION=1449

# ===========================================
# BACKUP CONFIGURATION
# ===========================================

# Enable automatic backups (1=yes, 0=no) (default: 1)
ENABLE_BACKUP=1

# Backup interval in minutes (default: 30)
BACKUP_INTERVAL=30

# Number of backup files to retain (default: 10)
BACKUP_RETAIN=10

# ===========================================
# WORLD CONFIGURATION (for new worlds)
# ===========================================

# World name (without .wld extension) (default: world)
WORLD_NAME=world

# Auto-create world if not found (1=yes, 0=no) (default: 1)
AUTO_CREATE=1

# World size (1=Small, 2=Medium, 3=Large) (default: 2)
WORLD_SIZE=2

# Difficulty (0=Classic, 1=Expert, 2=Master, 3=Journey) (default: 0)
DIFFICULTY=0

# World seed (leave empty for random seed)
SEED=

# ===========================================
# DOCKER VOLUME PATHS (Advanced)
# Usually don't need to change these
# ===========================================

# World path inside container (default: /worlds)
CONTAINER_WORLD_PATH=/worlds

# Config path inside container (default: /config)
CONTAINER_CONFIG_DIR=/config

# Backup path inside container (default: /backups)
CONTAINER_BACKUP_DIR=/backups

# Logs path inside container (default: /var/log)
CONTAINER_LOGS_DIR=/var/log